# Rust build output
/target/

# Stray compiled binary at repo root
/push

# Local config and state (never commit secrets or session state)
config.toml
# Keep legacy local configs ignored during migration.
config.json
state.json
/sessions/

# Local assistant memory (keep examples, ignore real personal data)
/assistant/Memory.md
/assistant/User.md

# OS / editor
.DS_Store
